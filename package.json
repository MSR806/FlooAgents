{
  "name": "flooagents",
  "private": true,
  "license": "MIT",
  "type": "module",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/MSR806/flooagents.git"
  },
  "bugs": {
    "url": "https://github.com/MSR806/flooagents/issues"
  },
  "homepage": "https://github.com/MSR806/flooagents#readme",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "prepare": "husky",
    "typecheck": "bun run --filter '*' typecheck",
    "test": "bun test",
    "lint": "biome check .",
    "format": "biome format --write .",
    "dev:harness": "bun run --filter '@flooagents/harness' dev",
    "dev:control-plane": "bun run --filter '@flooagents/control-plane' dev",
    "dev:gateway": "bun run --filter '@flooagents/gateway' dev",
    "dev:web": "bun run --filter '@flooagents/web' dev"
  },
  "dependencies": {
    "@flooagents/gateway-client": "workspace:*"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.0",
    "@types/bun": "^1.3.14",
    "husky": "^9.1.7",
    "typescript": "^6.0.3"
  }
}
